[project]
name = "flappy-nifty"
version = "0.1.0"
description = "Flappy Nifty browser game and shared leaderboard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=23.0.0",
    "python-dotenv>=1.0.1",
]
